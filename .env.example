# Telegram API credentials (get from https://my.telegram.org)
API_ID=123456
API_HASH=your_api_hash_here
BOT_TOKEN=your_bot_token_here

# MongoDB connection URI
MONGO_URI=mongodb://localhost:27017

# Bot owner user IDs (comma-separated)
OWNERS=123456789,987654321

# Optional: Monitored channels (comma-separated)
MONITORED_CHANNELS=

# Logging configuration
MAX_LOG_AGE_HOURS=24
MAX_LOG_SIZE_BYTES=2097152

# Optional: External OCR service URL (e.g., http://100.118.146.248:8181/api/ocr or http://localhost:8181/api/ocr)
# Leave empty to use Tesseract
EXTERNAL_OCR_SERVICE_URL=

# Optional: Port for the simple OCR service (default: 8181)
PORT=

# Webserver keep-alive port. Change this ONE value to move the port —
# docker-compose and webserver.py both read it automatically.
# Default: 5090
WEBSERVER_PORT=5090

# Default OCR method to attempt first (remaining method is tried as a fallback).
# Options: external | tesseract
# Default: tesseract
DEFAULT_OCR_METHOD=tesseract

# Whether to enable Tesseract as a local OCR option.
# Requires pytesseract and the system package tesseract-ocr to be installed.
# Set to false on low-resource or restricted machines to skip Tesseract entirely.
# Default: true
INSTALL_TESSERACT=true

# Optional: Hostname/label for this node in the HA cluster.
# Helps identify the machine in logs and MongoDB leader records.
# Auto-detected if omitted.
HOST_PC_NAME=

# Optional: Explicit outbound IP for this node, written into the MongoDB
# leader record so session.py can confirm only the leader starts Telethon.
# Auto-detected if omitted (recommended for most setups).
# Set this explicitly if your machine is behind NAT or uses a VPN.
NODE_IP=


