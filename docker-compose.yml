version: '3.8'

services:
  bot:
    build: .
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env:ro
      # Optional: Mount logs for persistence
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Kolkata
