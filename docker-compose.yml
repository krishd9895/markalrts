services:
  bot:
    build: .
    restart: unless-stopped
    hostname: ${NODE_LABEL:-}
    ports:
      - "5090:5090"
    volumes:
      - ./.env:/app/.env:ro
      # Optional: Mount logs for persistence
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Kolkata
