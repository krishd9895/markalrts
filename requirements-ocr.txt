# Optional local OCR stack. Only install if INSTALL_TESSERACT=true (default).
# If these fail to install (e.g. low-resource machine, missing system package
# tesseract-ocr), the bot still runs fine -- it falls back to Google Drive
# OCR and/or EXTERNAL_OCR_SERVICE_URL instead. See ocr.py for fallback logic.
# EasyOCR is not used in this repo.
Pillow
pytesseract
